trial_name: "unittest"
output_dir: "REPLACE!"

data_params:
  dataset: "REPLACE!"
  neighbor_mode: "REPLACE!"
  batch_size: 16
  working_size: 5
  lam: 10

model_params:
  name: "nlse"
  n_in: 4
  n_out: 2
  size: "small"
  verbose: yes

hyperparams:
  learning_rate: 0.02
  margin_same: 0.0
  margin_diff: 1.0
  origin_penalty: 0.0

train_params:
  max_iter: 100
  save_freq: 25
  print_freq: 5
  nan_exceptions: 10
